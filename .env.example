# Copy to .env and fill in. NEVER commit .env.

# Corpus root on the internal network share.
# Raw documents are referenced through this path and never enter the repo.
DOCEVAL_CORPUS_DIR=

# --- Phase 2+ (Azure), unused by `doceval scan` ---
AZURE_DI_ENDPOINT=
AZURE_DI_KEY=
AZURE_FOUNDRY_ENDPOINT=
AZURE_FOUNDRY_KEY=
AZURE_FOUNDRY_GPT_DEPLOYMENT=
AZURE_FOUNDRY_EMBEDDING_DEPLOYMENT=
